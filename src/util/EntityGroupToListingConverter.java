package util;

import models.EntityGroup;
import models.Listing;

@SuppressWarnings("unused")
public class EntityGroupToListingConverter {
	/*public Listing toListing(EntityGroup eg){
		Listing result = new Listing();
		eg.getAddress().getAddress()
		eg.getAddress().getCity()
		eg.getAddress().getCountry()
		eg.getAddress().getState()
		eg.getAddress().getZip_code()
		eg.getAddress().getZip_id()
	}*/
}
